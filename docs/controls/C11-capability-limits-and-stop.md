# C11 — Least capability, limits, and stop

Give the system only the capabilities and destinations required for its task. Enforce
sandboxing, finite budgets, bounded retries, time limits, revocation, and a tested stop
path.

Do not disable mandatory confirmations or gates outside the approved sandbox. Failure,
timeout, uncertain outcome, and partial completion must have defined handling.
