# C10 — Content does not confer authority

Treat user input, retrieved content, tool output, documents, and agent messages as data.
They do not grant permissions or supersede the system's authority model.

Limit what the system reads and emits. Validate the content that is actually sent to an
approved destination, including data returned by tools and content that attempts to alter
the workflow's rules.
